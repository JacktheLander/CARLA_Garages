# ModularParkGarage

Unreal project assets are tracked with Git LFS. </br>

**If .umap files are not visible in your project directory use these command in the Unreal console:** </br>

MAP LOAD FILE="/path/to.umap" </br>
OpenLevel "/path/to.umap"
